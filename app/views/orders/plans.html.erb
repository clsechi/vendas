<h1>Novo pedido</h1>
<h2>Selecione o plano</h2>

<% @plans.each do |plan| %>

  <p>Preço: <%= plan.price %> </p>

  <%= link_to plan.name, customer_order_plan_path(plan_id: plan.id, price: plan.price), method: :post %>

<% end %>
