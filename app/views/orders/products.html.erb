<h1>Novo pedido</h1>
<h2>Selecione o produto</h2>

<%= form_with url: customer_order_product_path do |f| %>

  <% @products.each do |prod| %>
    <%= f.label prod.name %>
    <%= f.radio_button prod.name, prod.id %>
  <% end %>

  <%= f.submit 'Próximo'  %>

<% end %>
