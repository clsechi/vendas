<h1>Novo pedido</h1>

<%= form_with url: customer_orders_path do |f| %>

  <% @categories.each do |cat| %>
    <%= f.radio_button cat.name, cat.id, id: :category_id %>
  <% end %>

  <%= f.submit 'Próximo'  %>

<% end %>
