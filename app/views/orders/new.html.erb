<%= render 'headbar' %>
<h1>Novo pedido</h1>

<% @categories.each do |cat| %>

  <%= link_to cat.name, customer_orders_path(category_id: cat.id,
    category_name: cat.name ), method: :post %>

<% end %>

<p><%= link_to 'Voltar', root_path, class:'ls-btn'%></p>
